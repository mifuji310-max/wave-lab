# AndroidからGitHubへ更新する手順

このZIPを解凍した後、リポジトリの画面から **Add file → Upload files** を選び、中のファイルとフォルダをアップロードします。

## 既存ファイルの扱い

`README.md` など同名ファイルが既にある場合は、今回のファイルで置き換えて構いません。

## 注意

- ZIPそのものをリポジトリに置くのではなく、解凍後の中身をアップロードします。
- `.github` は先頭がピリオドのため、Androidのファイル選択画面で見えない場合があります。
- `.github` が見えない場合は、まず通常のファイルをアップロードし、GitHub上で後から作成しても問題ありません。
- コミットメッセージは `Add initial project documentation` を推奨します。
